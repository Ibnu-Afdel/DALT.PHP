// Adds the jest-dom matchers (toBeInTheDocument, toBeDisabled, ...) to expect().
// Registered through vite.config.ts -> test.setupFiles, so every test file gets them.
import '@testing-library/jest-dom/vitest';
